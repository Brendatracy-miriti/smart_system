// seed-data.js — removed dummy data population for live data only
(function() {
  // No dummy data populated — system uses live registrations only
  console.info('Seed data disabled — using live data only.');
})();
